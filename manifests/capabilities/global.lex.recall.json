{
  "manifestVersion": "axf/v0",
  "id": "global.lex.recall",
  "summary": "Read-only recall example routed through the generic CLI adapter.",
  "provider": "lex",
  "adapterType": "cli",
  "executionTarget": {
    "command": "lex",
    "args": ["recall", "--json"]
  },
  "argsSchema": {
    "type": "object",
    "properties": {
      "query": { "type": "string" },
      "list": { "type": "integer", "minimum": 1 },
      "exact": { "type": "boolean" },
      "fold-radius": { "type": "integer", "minimum": 0 },
      "auto-radius": { "type": "boolean" },
      "max-tokens": { "type": "integer", "minimum": 1 }
    }
  },
  "outputModes": ["json"],
  "sideEffects": "read",
  "scope": "global",
  "lifecycleState": "active",
  "defaults": {},
  "policies": [],
  "owner": "module:lex",
  "examples": [
    "axf run lex recall --list 5",
    "axf run lex recall --query 'recent work'"
  ]
}
